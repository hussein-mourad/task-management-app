# General
PORT="8000"
BASE_URL="http://localhost:$PORT"

# PostgreSQL
DATABASE_URL="postgres://postgres:postgres@localhost:5432/postgres"

# Tests use a separate database automatically (task_app_test is created and
# migrated by vitest's globalSetup). No extra env vars required.

# Frontend
FRONTEND_URL="http://localhost:3000"

# Auth
JWT_SECRET=
